/*
Idea here is a component that automatically removes entities from the render and collision logic when off-screen,
AND adds them back when on-screen.

Component needs:
- Bounding box for screen intersection
*/
